\documentclass[../implementierung.tex]{subfiles}
\begin{document}
    \section{StepDetection-Library}
        \subsection{\#Input}
            \begin{itemize}
                \add[public int Datalength]{Datenlänge eines Paktes welches zum Algorithmus geschickt wird}
                \add[public int SamplingRate]{Anzahl der Datenmessungen pro Sekunde}
                \add[public Input()]{Konstruktor}
            \end{itemize}

        \subsection{\# AccGyroData}
            Ist jetzt eine Klasse
            \begin{itemize}
                \remove[short\array{} xacc, yacc, zacc, xgyro, ygyro, zgyro]{Wurden ersetzt durch \code{AccData} und \code{GyroData}}
                \add[public readonly AccData AccData]{Enthält Beschleuningsdaten}
                \add[public readonly GyroData GyroData]{Enthält Gyroscopedaten}
                \add[public int Datalength]{Datenlänge eines Paktes welches zum Algorithmus geschickt wird}
                \add[public int SamplingRate]{Anzahl der Datenmessungen pro Sekunde}
                \add[public double LengthInSeconds]{Länge eines Datenpakets in Sekunden}
                \add[public AccGyroData(int dataLength, int samplingRate)]{Konstruktor}
            \end{itemize}


        \subsection{+ AccData}
            Neues Struct mit Beschleuningsdaten
            \begin{itemize}
                \add[public double\array{} Xacc]{Beschleuninigunsdaten x-Achse}
                \add[public double\array{} Yacc]{Beschleuninigunsdaten y-Achse}
                \add[public double\array{} Zacc]{Beschleuninigunsdaten z-Achse}
                \add[public AccData(int a)]{Konstruktor mit Länge des Datenpakets}
                \add[public AccData(double\array{} xacc, double\array{} yacc, double\array{} zacc)]{Konstruktor mit Arrays}
            \end{itemize}



        \subsection{+ GyroData}
            Neues Struct mit Gyroscopedaten
            \begin{itemize}
                \add[public double\array{} Xgyro]{Gyroscopedaten x-Achse}
                \add[public double\array{} Ygyro]{Gyroscopedaten y-Achse}
                \add[public double\array{} Zgyro]{Gyroscopedaten z-Achse}
                \add[public GyroData(int a)]{Konstruktor mit Länge des Datenpakets}
                \add[public GyroData(double\array{} xgyro, double\array{} ygyro, double\array{} zgyro)]{Konstruktor mit Arrays}
            \end{itemize}

        \subsection{\# StepDetectionAlg}
            \begin{itemize}
                \add[public uint FrequencyResolution]{Anzahl der Datenpakete aus denen die Frequenz berechnet wird}
                \add[public StepDetectionAlg()]{Konstruktor für StepDetectionAlg}
            \end{itemize}

        \subsection{Output}
            Keine Änderungen

        \subsection{\# Outputmanager}
        	\begin{itemize}
	            \add[IDisposable Subscribe(IOutputObserver)]{Hier liegt eine erlaubte Kovarianz vor. Der OutputManager kann somit allen Subscribern 
	            die Frequenz über einen individuell anpassbaren Zeitraum anbieten.}
        	\end{itemize}
           
        \subsection{+ IOutputObserver}
        	Dieses Interface hat einen DesiredFrequencyInterval Um anzugeben, auf welche Zeiträume die Frequenz berechnet werden soll.
        	\sign{public interface IOutputObserver : Observer$<$Output$>$}
        	\begin{itemize}
        		\add[int DesiredFrequencyInterval]{Definiert eine Get Property}
        	\end{itemize}

\end{document}
